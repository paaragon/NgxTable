export default class DeepMerge {
    private static isMergeableObject;
    private static emptyTarget;
    private static cloneIfNecessary;
    private static defaultArrayMerge;
    private static mergeObject;
    static deepmerge(target: any, source: any, optionsArgument?: any): any;
    static deepmergeAll(array: any, optionsArgument: any): any;
}
